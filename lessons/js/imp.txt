Javascript is higher level programming language. It is interperted.
Javascript is used in web scriting, server (REST), mobile application(using React Native), Desktop application (Electron)
we can run Javascript 2 ways 
    1. using a client web browser (client-side)
    2. using node.js(runtime env) (server-side)
Dynamic typed language (data type is determined in the runtime).
Their are various frameworks and libraries that are written using Javascript to create/work UI/SERVER/MOBILE APPs (React, Express, React Native).


code 

syntax:
    OOP and POP

variable
    Empty Boxes -> name -> value/data

    scope name = value/data;

var(public), let(protected) and const(protected | immutable)

let age = 23; -> number
let name = "amey khondekar"; -> string
let pass = true; -> boolean

value/data: 

    data types 
        1. primitive (number[int[4bytes], float[4bytes], double[8bytes]], string(group of chars[1byte]), boolean[1byte](single value true/false), undefined, NaN)
        2. non primitive (array, object, function, etc.)

    RAM
        address

oprators:

    1. airthmatic oprators : +, -, *, /, % 
    2. increement and decrement oprators  : pre/post --(-1), ++(+1)
    3. comparaision oprators : < , > , <=, >=, !=, ==, ===
    4. logical oprators : && AND, || OR
    5. assignment oprators: =, +=,-=,/=, *=, %=
    6. ternary oprator : ? (ternary statements)
    7. member access oprator : " . "

statements | expresions

input/output statements

Javascript it has built in input and output methods

output : console.log, window.alert, document.write, etc.. 
input : window.prompt

conditional statements

how to run js program. client side (using browser), server side(using node.js)

in Javascript document means the html page where it is connected !